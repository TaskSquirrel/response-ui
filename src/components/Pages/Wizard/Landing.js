import React from "react";
import Button from "../../Form/Button";

const Landing = () => (
    <div>
        <h1>
            Call History Analysis
        </h1>
        <div>
            <Button>
                Start uploading
            </Button>
        </div>
    </div>
);

export default Landing;
